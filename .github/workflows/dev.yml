# The dev channel: every push to main becomes its own immutable
# version — gzipped objects in the fast train's bucket, keyed by that
# version, never overwritten and never reused — and a freshly signed
# manifest on the update host. Nothing rolls: history stays
# addressable, so pinning a relay on an older dev build to bisect a
# regression is one manifest away, and the weekly sweep is what
# forgets, deliberately, manifest in hand.
name: dev channel
on:
  push:
    branches: [main]
concurrency:
  group: dev-channel
  cancel-in-progress: true

jobs:
  version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.v.outputs.version }}
    steps:
      - uses: actions/checkout@v4
      - id: v
        # The date is the COMMIT's, not the job's: the version is a
        # function of the source, and a rerun mints the same identity.
        run: |
          stamp="$(git show -s --format=%cd --date=format-local:%Y%m%d%H%M%S "$GITHUB_SHA")"
          echo "version=0.0.0-dev.${stamp}.${GITHUB_SHA::12}" >> "$GITHUB_OUTPUT"
        env:
          TZ: UTC

  build:
    needs: version
    uses: ./.github/workflows/build.yml
    with:
      version: ${{ needs.version.outputs.version }}
      expected_sha: ${{ github.sha }}

  publish:
    needs: [version, build]
    runs-on: ubuntu-latest
    environment: fast
    permissions:
      contents: read
    env:
      VERSION: ${{ needs.version.outputs.version }}
      PRODUCT: ${{ needs.build.outputs.slug }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true
      - name: Refuse to publish unsigned channels
        run: grep -q "untrusted comment" internal/update/trust.go ||
          { echo "no embedded official key — mint one and embed it first"; exit 1; }
      - name: Compress, sign, upload, publish
        env:
          RELSIGN_KEY: ${{ secrets.RELSIGN_KEY }}
          DEPLOY_KEY: ${{ secrets.UPDATES_DEPLOY_KEY }}
          AWS_ACCESS_KEY_ID: ${{ secrets.S3_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.S3_SECRET_KEY }}
          AWS_REGION: ${{ vars.S3_REGION || 'us-east-1' }}
          S3_ENDPOINT: ${{ vars.S3_ENDPOINT }}
          S3_BUCKET: ${{ vars.S3_BUCKET }}
          DL_BASE: ${{ vars.DL_BASE }}
        run: |
          # The bucket wiring is the environment's to provide — absent
          # values must fail here, in one line, not as a bent URL or a
          # cryptic aws error three steps later.
          : "${DL_BASE:?the environment must name its public download base}"
          for f in "${PRODUCT}_linux_arm64" "${PRODUCT}_linux_amd64" "${PRODUCT}_linux_arm"; do
            mv "$f" "${f/${PRODUCT}_/${PRODUCT}_${VERSION}_}"
          done
          base="${DL_BASE}/${PRODUCT}/${VERSION}"
          printf '%s' "$RELSIGN_KEY" > relsign.key
          # relsign -gzip compresses and hashes in one process: the .gz
          # files it writes are byte-for-byte what the signature vouches
          # for, and they are what gets uploaded.
          go run ./cmd/relsign manifest -gzip dev "$VERSION" manifest.json \
            "linux/arm64=${base}/${PRODUCT}_${VERSION}_linux_arm64.gz=${PRODUCT}_${VERSION}_linux_arm64" \
            "linux/amd64=${base}/${PRODUCT}_${VERSION}_linux_amd64.gz=${PRODUCT}_${VERSION}_linux_amd64" \
            "linux/arm=${base}/${PRODUCT}_${VERSION}_linux_arm.gz=${PRODUCT}_${VERSION}_linux_arm"
          go run ./cmd/relsign sign relsign.key manifest.json
          # Artifacts land before the manifest that names them: the
          # published channel never points at bytes not yet there.
          for f in "${PRODUCT}_${VERSION}"_linux_*.gz; do
            ./.github/scripts/s3-put-immutable.sh "${PRODUCT}/${VERSION}/${f}" "$f"
          done
          ./.github/scripts/publish-manifests.sh dev
